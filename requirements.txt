# API-server runtime deps (core web stack). Installs cleanly on any Python 3.10+.
# The heavy video-analysis deps (opencv-python-headless, openai-whisper, numpy,
# Pillow, mcp) are declared in pyproject.toml and imported lazily — install them
# with `pip install -e .` when you need the film-analysis / transcribe routes.
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
sqlalchemy>=2.0.0
python-multipart>=0.0.9
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.0
aiofiles>=23.0.0
openpyxl>=3.1.0
python-docx>=1.1.0
google-auth>=2.28.0
# google-auth declares requests as an optional extra, but google.auth.transport
# .requests — the transport used to verify an ID token against Google's public
# keys — imports it at module scope. Without it google-auth installs happily and
# then raises ImportError at the moment someone signs in, which api/google_auth
# reports as "Google sign-in support is not installed on this server yet".
requests>=2.31.0
boto3>=1.34.0
anthropic>=0.40.0
# Server-side PDF export. Pure Python on purpose: WeasyPrint and its relatives
# need cairo and pango on the machine, which on Railway is a build to babysit
# rather than a line in this file. xhtml2pdf renders the same print document
# the app builds, so an exported file matches a printed page exactly.
xhtml2pdf>=0.2.16
